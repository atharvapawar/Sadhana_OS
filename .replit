run = "npm run dev"
entrypoint = "src/main.tsx"

[nix]
channel = "stable-24_05"

[deployment]
build = ["sh", "-c", "npm ci && npm run build"]
run = ["sh", "-c", "npm run preview -- --host 0.0.0.0 --port ${PORT:-5173}"]
